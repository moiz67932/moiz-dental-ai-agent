# =============================================================================
# Moiz Dental AI Agent — Production Verified Requirements
# =============================================================================

# Core SDK (Stable 1.x series)
livekit==1.0.23
livekit-agents[silero,openai,cartesia,deepgram]==1.3.11
livekit-api==1.1.0
livekit-protocol==1.1.1

# AI Plugins (Locked to core version for stability)
livekit-plugins-openai==1.3.11
livekit-plugins-cartesia==1.3.11
livekit-plugins-deepgram==1.3.11
livekit-plugins-silero==1.3.11

# LLM & Authentication
# Note: Keep openai < 2.0.0 to prevent plugin dependency breaks
openai>=1.40.0,<2.0.0
google-api-python-client>=2.140.0
google-auth>=2.32.0
google-auth-oauthlib>=1.2.4
google-auth-httplib2>=0.3.0

# Database & Infrastructure
supabase>=2.10.0
python-dotenv>=1.0.1

# Utilities & Parsing
phonenumbers>=8.13.40
email-validator>=2.2.0
python-dateutil>=2.9.0
tzdata>=2025.1
aiohttp>=3.10.0